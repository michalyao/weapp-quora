<!--index.wxml-->

<view class="container" animation= "{{animation}}" bindtap="getQuote">
  <view class="quote-text" animation="{{ animation }}" >
    <text class="content" animation="{{ animation }}">{{ text }}</text>
    <view class="quote-author"> by {{author}} </view>
  </view>
</view>